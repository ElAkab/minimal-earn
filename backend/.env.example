# Configuration Ollama

# Timeout pour les requêtes Ollama (en millisecondes)
# Valeurs recommandées :
# - 0 : Pas de timeout (recommandé pour Ollama local avec modèles lourds)
# - 120000 : 2 minutes (si tu veux une limite de sécurité)
# - 300000 : 5 minutes (pour les très gros modèles)
#
# Note : Les modèles 20B+ peuvent prendre 60-90 secondes pour générer une réponse
OLLAMA_TIMEOUT=0

# Port du serveur backend
PORT=5000
